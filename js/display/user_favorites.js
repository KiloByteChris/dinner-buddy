/**
 *  Display user Favorites
 */
function user_favorites_view() {
    var favView = '';
    // Add stuff to enhance search (tags, catagories)
    favView += '<div id="userFavoritesView">';
    favView += '</div>';
    return favView;
}